name: Farms
version: '0.0.1'
main: com.github.triikow.farms.Farms
api-version: '1.21'
softdepend: [WorldEdit]

permissions:
  farms.admin.reload:
    description: Allows reloading the Farms config
    default: op
  farms.admin.setup:
    description: Allows using /farm admin setup to create void world
    default: op
